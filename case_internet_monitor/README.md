# CASE Internet Monitor

This add-on records lightweight ICMP and HTTP/HTTPS measurements locally in `/data/internet_monitor.sqlite3`. It intentionally avoids continuous speed tests and does not export PingPlotter files in V1.
