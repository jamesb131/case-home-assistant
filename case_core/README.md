# CASE Core

CASE Core runs the household assistant API, worker and web UI on Home Assistant
OS. It expects a reachable Postgres database, normally the companion CASE
Postgres app.

This app is experimental. Keep a Home Assistant backup before installing it on
the Green.
